function difference(speedX, speedY){
    let distance1 = speedX * (2 + 3);
    let distance2 = speedY * 3;
    let diff = Math.abs(distance1 - distance2);
    console.log(diff);
}

difference(81, 120);